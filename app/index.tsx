import React from 'react';
import { View } from 'react-native';
import { Header, TabViewComponent } from './components';

const App = () => (
    <View style={{ flex: 1 }}>
        <TabViewComponent />
    </View>
);

export default App;
